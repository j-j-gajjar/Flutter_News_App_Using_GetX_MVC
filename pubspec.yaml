name: newsappflutter
description: A new latest and trendy news for you on a finger tip with advanced with advanced features and its FREE .

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  http: ^0.13.4
  webview_flutter: ^2.1.1
  get: ^4.3.8
  json_annotation: ^4.1.0
  cached_network_image: ^3.1.0

dev_dependencies:
  build_runner: ^2.1.4
  json_serializable: ^5.0.2
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
